// Endless Runner Game
// Var canvas = document.getElementById('canvas');
// Set the canvas dimensions; canvas.width =400; canvas .height =400;
// Get the canvas context; var ctx = canvas . getContext ('2d');
//Player character; var player ={ x: 50 , y: canvas .height / 20. speed: 5
// Draw the player; ctx.fillstyle ='blue'; ctx.fillRect(player.x,player.y,player.width,player.height);
//Handle key presses document.addEventListener('Keydown ',functions (event) { ;  switch (event.key) { ;     case 'ArrowLeft':  player.x -= player speed; break;           }                 });
